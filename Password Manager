
def add_note():
    create_note = input('Add Note : Press 3 ')
    if create_note == str(3):
        Username = input('Enter Username')
        Password = input('Enter Password')
        Website = input('Enter Web Address')
        print([Username,Password,Website])

add_note()
def retrieve_note():
    pull_note = input('What note would you like to retrieve.')
